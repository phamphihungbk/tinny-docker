version: '3'

networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge

services:
  workspace-node:
    image: web-node:latest
    build:
      context: ./workspace-node
    container_name: web-node
    working_dir: /web
    volumes:
    - ../web:/web
    tty: true
    networks:
      - frontend
      - backend

  workspace-php:
    image: web-php:lastest
    build:
      context: ./workspace-php
    container_name: web-php
    working_dir: /web
    volumes:
      - ../web:/web
    tty: true
    networks:
      - frontend
      - backend

  nginx:
    image: web-nginx:latest
    build:
      context: ./nginx
    container_name: web-nginx
    working_dir: /web
    ports:
    - 80:80
    - 443:443
    networks:
      - frontend
      - backend

  mysql:
    image: web-mysql:latest
    build:
      context: ./mysql
    container_name: web-mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=secret
    volumes:
    - ./mysql/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    - ./mysql/data:/var/lib/mysql
    networks:
      - backend

  phpmyadmin:
    image: web-mysql-admin:lastest
    build:
      context: ./phpmyadmin
    container_name: web-mysql-admin
    restart: always
    depends_on:
      - mysql
    environment:
      - PMA_HOST=web-mysql
      - PMA_ABSOLUTE_URI=http://admin.tiny-docker.local/
    networks:
      - backend
